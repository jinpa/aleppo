# ─── Aleppo Environment Variables ────────────────────────────────────────────
# Copy this file to .env.local for local development.
# On Railway, set these in the project's Variables tab.

# ── Database ──────────────────────────────────────────────────────────────────
# Local: postgresql://localhost/aleppo  (Homebrew PostgreSQL, no password)
# Railway: automatically set as $DATABASE_URL by the Postgres add-on
DATABASE_URL="postgresql://localhost/aleppo"

# ── Auth.js ───────────────────────────────────────────────────────────────────
# Generate: openssl rand -base64 32
AUTH_SECRET=""
NEXTAUTH_SECRET=""   # same value as AUTH_SECRET (some adapters check this)

# ── Google OAuth (optional) ───────────────────────────────────────────────────
# Create at console.cloud.google.com → APIs & Services → Credentials
# Authorized redirect URIs:
#   Local:   http://localhost:3000/api/auth/callback/google
#   Railway: https://your-app.railway.app/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# ── Cloudflare R2 (optional — images fall back to placeholder without this) ───
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="aleppo-images"
# Public URL for your R2 bucket (e.g. https://pub-xxx.r2.dev or custom domain)
R2_PUBLIC_URL=""

# ── App ───────────────────────────────────────────────────────────────────────
# Local: http://localhost:3000
# Railway: https://your-app.railway.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"
